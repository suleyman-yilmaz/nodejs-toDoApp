<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do App | Register</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@2.5.0/fonts/remixicon.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            transition: background-color 0.3s, color 0.3s;
            background-color: var(--body-background);
        }

        .container {
            max-width: 400px;
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 0 17px 10px rgb(0 0 0 / 30%);
            background-color: var(--container-background);
            color: var(--text-color);
            position: relative;
        }

        .btn-primary {
            background: var(--btn-background);
            border: none;
            border-radius: 0.5rem;
        }

        .form-label {
            color: var(--text-color);
        }

        .night-mode {
            --body-background: #343a40;
            --container-background: #495057;
            --text-color: #f8f9fa;
            --btn-background: #007bff; /* Gece modunda buton rengi */
            --link-color: #f8f9fa; /* Gece modunda bağlantı rengi */
        }

        .day-mode {
            --body-background: #ffffff;
            --container-background: #ffffff;
            --text-color: #212529;
            --btn-background: #007bff; /* Gündüz modunda buton rengi */
            --link-color: #007bff; /* Gündüz modunda bağlantı rengi */
        }

        .theme-toggle {
            position: absolute;
            top: 1rem;
            right: 1rem;
            cursor: pointer;
        }

        .theme-toggle i {
            font-size: 1.5rem;
            color: var(--text-color);
        }

        .text-muted {
            color: var(--link-color) !important;
        }

        .form-label {
            margin-bottom: 0.5rem;
        }

        .text-input {
            background: #e6e6e6;
            height: 40px;
            display: flex;
            align-items: center;
            border-radius: 10px;
            padding: 0 15px;
            margin: 5px 0;
        }

        .text-input input {
            background: none;
            border: none;
            outline: none;
            width: 100%;
            height: 100%;
            margin-left: 10px;
        }

        .text-input i {
            color: #686868;
        }

        ::placeholder {
            color: #9a9a9a;
        }

        .register-btn {
            width: 100%;
            padding: 10px;
            color: white;
            background: var(--btn-background);
            border: none;
            border-radius: 0.5rem;
            cursor: pointer;
            margin-top: 10px;
        }

        a {
            font-size: 12px;
            color: var(--link-color);
            cursor: pointer;
            user-select: none;
            text-decoration: none;
        }

        a.login {
            margin-top: 15px;
        }

        @media (min-width: 768px) {
            .container {
                width: 50vw;
                height: 60vh;
                display: grid;
                grid-template-columns: 100%;
                grid-template-areas: "register";
                box-shadow: 0 0 17px 10px rgb(0 0 0 / 30%);
                border-radius: 20px;
                background: var(--container-background);
            }

            .register {
                grid-area: register;
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                position: relative;
                background: var(--container-background);
            }
        }
    </style>
</head>

<body class="day-mode">
    <div class="theme-toggle" onclick="toggleTheme()">
        <i class="ri-moon-fill" id="theme-icon"></i>
    </div>
    <div class="container">
        <div class="register">
            <h3 class="title">Hesap Oluştur</h3>
            <form action="/register" method="post">
                <div class="text-input">
                    <i class="ri-user-fill"></i>
                    <input type="text" placeholder="Kullanıcı Adı" name="username" required>
                </div>
                <div class="text-input">
                    <i class="ri-lock-fill"></i>
                    <input type="password" placeholder="Şifre" name="password" required>
                </div>
                <button type="submit" class="register-btn">Üye Ol</button>
            </form>
            <div class="mt-3 text-center">
                <a href="/" class="text-muted">Zaten hesabım var.</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function toggleTheme() {
            const body = document.body;
            const themeIcon = document.getElementById('theme-icon');
            if (body.classList.contains('night-mode')) {
                body.classList.remove('night-mode');
                body.classList.add('day-mode');
                themeIcon.classList.remove('ri-sun-fill');
                themeIcon.classList.add('ri-moon-fill');
            } else {
                body.classList.remove('day-mode');
                body.classList.add('night-mode');
                themeIcon.classList.remove('ri-moon-fill');
                themeIcon.classList.add('ri-sun-fill');
            }
        }
    </script>
</body>

</html>